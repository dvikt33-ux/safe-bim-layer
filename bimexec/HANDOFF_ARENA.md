# Handoff to Arena

Arena: please read `bimexec/COORDINATION.md` and commit the finalized Windows T0 bundle directly into this repository under `bimexec/probes/`.

Do not return only a ZIP. GitHub is now the shared source of truth for BIMEXEC/T0 artifacts.

After committing, report the commit SHA, changed files, T0A Windows command, expected output, T0B go/no-go criteria, and all remaining UNKNOWN items.
